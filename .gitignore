*.exe
*.EXE
*.exe
software/rsrc_windows_*.syso
/firmware/out
/firmware/build
host/cmd/bridge/rsrc_windows_*.syso
/firmware-idf/build
/firmware-idf/sdkconfig
/firmware-idf/sdkconfig.old
/firmware-idf/tools/.venv
# macOS host build output (build-macos.sh). The legacy software-macos/ once
# had two 29 MB binaries committed for want of these rules.
/host/build
/host/dist
*.app
.DS_Store
# Raw device-catalog export that host/internal/devicedb/gen reads. Only the
# generated devices.tsv is committed.
/devices.csv
# Code-signing material. The certificate lives in the keychain and in CI
# secrets (host/packaging/macos/make-signing-cert.sh); it must never land
# in this public repository in any form.
*.p12
*.pfx
*.cer
*.pem
*.key
*.keychain
*.keychain-db
